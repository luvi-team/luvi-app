name: luvi_core
description: Shared core utilities for LUVI app packages.
publish_to: 'none'

environment:
  sdk: ">=3.10.0 <4.0.0"

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
