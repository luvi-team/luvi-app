/// Opacity tokens used across the UI.
class OpacityTokens {
  static const double inactive = 0.2; // e.g., inactive dots

  /// Loading overlay background opacity (70%)
  /// Used in: auth_signin_screen.dart loading overlay
  static const double loadingOverlay = 0.7;

  OpacityTokens._();
}
